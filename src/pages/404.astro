---
import { onRequest } from '@/middleware';

export const prerender = false;

const preferredLang = Astro.cookies.get('preferred_lang')?.value || 'id';

return Astro.redirect(`/${preferredLang}/404`);
---