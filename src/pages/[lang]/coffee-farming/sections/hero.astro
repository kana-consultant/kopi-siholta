---
import PageHero from '@/components/shared/page-hero.astro';
import farmingBg from '@/images/bg/bg-farming.jpg';

interface Props {
  alt: string;
  titleLine1: string;
  titleLine2: string;
  subtitle: string;
  pageLabel: string;
  homeLabel: string;
  prevLabel: string;
  prevHref: string;
  ctaText: string;
  lang: string;
}
const { alt, titleLine1, titleLine2, subtitle, pageLabel, homeLabel, prevLabel, prevHref, ctaText, lang } = Astro.props;
---
<PageHero
  bgImage={farmingBg}
  alt={alt}
  titleLine1={titleLine1}
  titleLine2={titleLine2}
  subtitle={subtitle}
  pageLabel={pageLabel}
  homeLabel={homeLabel}
  prevLabel={prevLabel}
  prevHref={prevHref}
  overlayPosition="center 25%"
  showCta
  ctaText={ctaText}
  lang={lang}
/>
